import type { Meta } from '@storybook/react';
import { Align } from './align';

const Story: Meta<typeof Align> = {
  component: Align,
  title: 'Align',
};
export default Story;

export const Primary = {
  args: {},
};
