import type { Meta } from '@storybook/react';
import { Distribute } from './distribute';

const Story: Meta<typeof Distribute> = {
  component: Distribute,
  title: 'Distribute',
};
export default Story;

export const Primary = {
  args: {},
};
