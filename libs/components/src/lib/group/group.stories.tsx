import type { Meta } from '@storybook/react';
import { Group } from './group';

const Story: Meta<typeof Group> = {
  component: Group,
  title: 'Group',
};
export default Story;

export const Primary = {
  args: {},
};
